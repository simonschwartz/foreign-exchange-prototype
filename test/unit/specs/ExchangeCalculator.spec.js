import Vue from 'vue'
import Hello from 'src/components/ExchangeCalculator'

describe('ExchangeCalculator.vue', () => {
  it('should render correct contents', () => {
    /*
    const vm = new Vue({
      el: document.createElement('div'),
      render: (h) => h(ExchangeCalculator)
    })
    expect(vm.$el.querySelector('.exchange-calculator').textContent)
      //.to.equal('Welcome to Your Vue.js App')
    */
  })
})
